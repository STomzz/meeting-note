# 本地构建的私有配置（此文件已被 .gitignore 忽略，不会提交）
#
# 复制为 local.env 后填自己的后端地址：
#   cp local.env.example local.env
#
# 用途：build_apk.sh 会读这里的值，作为 --dart-define=MMA_BACKEND_BASE_URL=... 注入 APK，
#      让打包出来的 App 默认就连到你的后端；不填则默认 http://127.0.0.1:8000（本机开发）。
MMA_BACKEND_BASE_URL=http://YOUR_SERVER_IP:8000
