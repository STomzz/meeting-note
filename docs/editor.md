# 笔记编辑器：所见即所得（块编辑）

> 目标：正文**默认显示排版后的样子**（不显示 `#`、`-` 这些 md 原文），点一段就能改，改完立刻回到排版。
> 关键约束：**只替换被编辑的那一块**——会议笔记里的 `/v` 引用行、`> 🎙 转写` 块、
> `## 会议纪要（AI 整理）` 都必须逐字节保留（第三方 md 编辑器常见的「顺手重排」在这里是致命的）。

## 两种形态

工具栏右侧是**一个圆角按钮**，里面两个符号（v0.3.7 起不用文字）：`[]` = 编辑（所见即所得），`</>` = 源码；
点哪个符号切到哪个，当前形态高亮。选择记在 localStorage `bnu-notes-editor-mode`：

| 形态 | 说明 |
| --- | --- |
| **编辑** `[]`（默认） | `BlockEditor`：正文按块渲染，点段落进入编辑（无边框，只有光标闪）；输入 `#` / `##` 弹块类型菜单、`/v` 弹录音选择器。**编辑态与写回后的排版一致**：宽度只由面板决定（`.blocks { flex: 1 1 auto; min-width: 0 }`，v0.3.5 修），字体、行高、折行位置都相同——长行不会「半屏就换行」 |
| **源码** `</>` | textarea：直接改 markdown 原文，支持行首 `/v` 弹录音选择器（Ctrl+S 保存不变） |

两种形态共用同一份内容（`stores/notes.ts` 的 `content`），自动保存、问答定位、录音引用插入都走同一条路；
早先版本存的 `wysiwyg` / `preview` 会兼容读成「编辑」（v0.3.5 去掉了「预览」：它就是块编辑的只读形态，与编辑重复）。

## 块模型（`src/core/blocks.ts`）

- **块** = 一段连续的非空行；**空行**、**代码围栏**（``` / ~~~）、**`/v` 引用行**各自成块。
  空行块与围栏块不可编辑（空行块只用于还原原文）。
- 不变式：`joinBlocks(splitBlocks(md)) === md`（任何输入都逐字节相等，含尾随换行），单测里用会议笔记样例、
  代码围栏、表格、没闭合的围栏等一堆输入验证。
- `replaceBlock(blocks, id, text)` 只换目标块（新文本里有空行会再切块）；**未改动的块 id 与文本都不变**
  （id 是编辑句柄，稳定才能「提交完立刻点另一段」）。
- 其它：`appendBlocks` / `insertBlocksBefore` / `insertParagraphAfter` / `dropBlock` /
  `dropBlockTidy`（删一行后把并到一起的空行合成一个，「⋯ → 删除引用」用）。

## `/v` 录音选择器（源码模式与块编辑共用）

候选列表在 `src/core/refPicker.ts`（两边共用同一套排序与过滤，避免两处逻辑漂移）：

- 排序：**本笔记音频目录的** → 别的笔记的；同档里**没引用过的** → 已引用；再按修改时间新 → 旧；
- 过滤：按 `目录/文件名` 子串（大小写不敏感），最多显示 8 条；
- 每行显示 `已引用 / 未引用` 标签、`目录/文件名`、时长；`↑↓` 选、`Enter` 插入、`Esc` 关掉（只关弹层，不退出编辑）；
- 选中的那一刻，把正在输入的 `/v …` 整段换成完整引用行 `/v <相对路径>`（块里其余文字不动）。

块编辑里引用行渲染成播放器，行右侧有个 `⋯`（桌面悬停出现、触屏常显）。
播放器分两种（v0.3.7）：

- **整场引用**（`/v 会议音频/<笔记>/<场次>/`，行尾斜杠 = 目录）：渲染成**一条长语音**——
  一个播放键 + **整场时间轴**进度条（能直接拖到任意位置，内部换算成「第几段 + 段内偏移」）+ `第 x/y 段`，
  底下小字显示场次与已播/总时长。底层按序号顺序播分段 WAV，不转码、不重复占空间；
  实现是自定义元素 `bnu-session-player`（`src/core/sessionPlayer.ts`，时间轴换算在 `src/core/playlist.ts`，
  纯函数有单测）——块编辑器把每块渲染成 HTML 字符串，只有自定义元素能在 `v-html` 里被浏览器升级成活的控件；
- **单段引用**（`/v …/seg_0001.wav`，老笔记常见）：仍是原生 `<audio controls>`，一行一个。

| 菜单项 | 行为 |
| --- | --- |
| **删除引用（音频文件保留）** | 只删文档里这一行 `/v`（用 `dropBlockTidy` 合并相邻空行），**不动** `会议音频/` 下的 wav |
| **复制音频路径** | 复制 vault 相对路径（粘到资源管理器 / `ffmpeg` 命令里能用） |

## 交互（`src/components/BlockEditor.vue`）

| 操作 | 行为 |
| --- | --- |
| 点某一段 | 进入编辑；输入框里是**这一段**的原文（无边框、背景透明，只有光标闪），其它段保持排版 |
| 编辑态字号 | 按块类型走（`k-h1` / `k-h2` / `k-h3` / `k-ul` / `k-ol` / `k-quote` / `k-code`），和渲染结果一致，不跳字 |
| 编辑态宽度 | 块编辑器占满编辑列（`.blocks { flex: 1 1 auto }`），宽度只由面板决定、不由内容决定——长行在编辑态与写回后的折行位置完全一致 |
| 编辑态高度 | 输入框高度跟着**折行之后**的内容长（`autosizeTa`，进编辑 / 每次输入 / 文档宽度变化都重算）——长行折行时不会把行首顶出去 |
| 空段里输入 `/v`、`/video` | 弹出录音选择器（见上节） |
| 点别处 / `Ctrl+Enter` | 写回（内容变了才发 `update:content`） |
| `Esc` | 取消本次编辑，不发内容 |
| `Enter`（单行） | 以光标为界拆成两段；列表 / 引用自动续标记；只写了标记（`- `、`1. `、`# `）则退出标记变普通段落 |
| 空段里输入 `#` / `##` / `/` | 弹出块类型菜单：正文 / 一级标题 / 二级标题 / 三级标题 / 项目符号 / 有序列表 / 引用 / 代码块（↑↓ 选，Enter 应用） |
| 第一次进编辑 | 下方露一次操作提示（8 秒后自己消失，之后不再打扰） |
| `＋ 新起一段` | 文末新起段落 |
| 拖选文字 | 不进编辑（方便复制）；播放器、链接、按钮的点击也不进编辑 |
| 问答/图谱点引用 | `locate()` 滚动到目标块并高亮 1.8 秒 |

## 左侧两栏都能收起（v0.3.7）

| 栏 | 收起方式 | 记在哪 |
| --- | --- | --- |
| 文件树（笔记列表） | 列表右上角的 `‹` 按钮收起；收起后页头左侧出现 `›` 按钮可再展开（编辑区吃满宽度） | localStorage `bnu-notes-list-collapsed` |
| 应用导航（侧栏） | 侧栏顶部品牌行右侧的 `‹` 按钮；收起后只留图标（56px），同一个按钮变 `›` 再展开 | localStorage `bnu-notes-nav-collapsed` |

都不影响数据，纯粹是把宽度让给正文。

## 引用插入落点

录音面板 / 会议面板插入 `/v` 时（`insertLines`）：

1. 正在编辑某段 → 插到**那段之后**；
2. 否则若存在 `## 会议纪要（AI 整理）` → 插到**标题之前**；
3. 否则追加到文末。

第 2 条很重要：落到纪要段里的引用会在下一次「一键处理」时被整段覆盖（Rust 侧 `upsert_minutes`）。

## 测试

```bash
npx vitest run src/core/blocks.spec.ts             # 块模型：逐字节还原、编辑隔离、预设、插入落点、删除引用合并空行
npx vitest run src/core/refPicker.spec.ts          # 录音候选：排序、过滤、引用行文本
npx vitest run src/components/BlockEditor.spec.ts  # 组件交互：点选编辑、blur 写回、块类型菜单、`/v` 选择器、`⋯` 删除引用、定位
```

jsdom 测不出的那部分（焦点/blur 时序、浏览器补发的 blur、canvas 尺寸、G6/three 生命周期）用**真浏览器冒烟**兜：

```bash
npm run dev                  # 另开终端
node scripts/smoke-ui.mjs    # 真 Chrome 点一遍：块编辑 + /v 选择器 + 引用「⋯」+ 录音面板（假麦克风）+ 图谱 2D/3D
```

> v0.3.1 就是这么抓到一个 jsdom 测不出的 bug：Chrome 在旧输入框被移除 / 新输入框刚挂载时会补发 blur，
> 之前会把「回车拆段」刚打开的新编辑会话立刻关掉（写回逻辑因此改成只认当前这一轮编辑的输入框元素）。

## 已知限制

- 整场播放器是「分段顺序播」，跨段瞬间会有极短的间隙；拖进度条按**已加载的分段时长**换算
  （WAV 头里读出来的时长，播放器拿到媒体元数据后会自己校正）；
- 行内格式（加粗、链接）没有悬浮工具栏：在段里直接写 `**加粗**`、`[文字](url)`，写回后即渲染；
- 未做块拖拽排序、多选块、表格编辑 UI（表格块按原文编辑）；
- 编辑器只用于笔记正文；转写块与纪要段由「一键处理」生成，建议在源码形态里改（或在块里直接改也可以，不影响解析）。
